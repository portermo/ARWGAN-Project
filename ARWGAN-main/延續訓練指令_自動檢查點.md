# 延續訓練指令（自動使用最新檢查點）

## 🚀 快速開始

### 方法 1: 使用自動檢測腳本（最簡單）

```bash
cd /mnt/nvme/p3/Project/arwgan/ARWGAN-Project/ARWGAN-main

./延續訓練_自動檢查點.sh
```

這個腳本會：
- ✅ 自動找到最新的 `checkpoint_epoch_*.pth`
- ✅ 如果沒有找到，會嘗試使用 `best_model.pth`
- ✅ 如果都沒有，會從頭開始訓練
- ✅ 使用優化的參數（epochs=100, batch=24, use_vgg）

---

### 方法 2: 一行命令（自動檢測最新檢查點）

```bash
cd /mnt/nvme/p3/Project/arwgan/ARWGAN-Project/ARWGAN-main

LATEST=$(ls -t checkpoints_improved/checkpoint_epoch_*.pth 2>/dev/null | head -1) && \
./run_training.sh --train --epochs 100 --batch 24 --use_vgg --data-dir data/coco2017 --lr 1e-4 ${LATEST:+--resume $LATEST}
```

**說明**：
- `LATEST` 變量會自動找到最新的檢查點
- `${LATEST:+--resume $LATEST}` 表示如果 LATEST 存在，就添加 `--resume` 參數
- 如果沒有檢查點，會從頭開始訓練

---

### 方法 3: 更完整的自動檢測命令

```bash
cd /mnt/nvme/p3/Project/arwgan/ARWGAN-Project/ARWGAN-main

# 自動檢測最新檢查點
LATEST_CHECKPOINT=$(ls -t checkpoints_improved/checkpoint_epoch_*.pth 2>/dev/null | head -1)

# 如果沒有找到，嘗試 best_model.pth
if [ -z "$LATEST_CHECKPOINT" ]; then
    LATEST_CHECKPOINT="checkpoints_improved/best_model.pth"
fi

# 執行訓練
if [ -f "$LATEST_CHECKPOINT" ]; then
    echo "使用檢查點: $LATEST_CHECKPOINT"
    ./run_training.sh --train --epochs 100 --batch 24 --use_vgg --data-dir data/coco2017 --lr 1e-4 --resume "$LATEST_CHECKPOINT"
else
    echo "未找到檢查點，從頭開始訓練"
    ./run_training.sh --train --epochs 100 --batch 24 --use_vgg --data-dir data/coco2017 --lr 1e-4
fi
```

---

## 📋 推薦使用方式

### 最簡單（推薦）

```bash
cd /mnt/nvme/p3/Project/arwgan/ARWGAN-Project/ARWGAN-main
./延續訓練_自動檢查點.sh
```

### 自定義參數

修改 `延續訓練_自動檢查點.sh` 中的參數：
- `EPOCHS=100` - 總訓練 epochs
- `BATCH_SIZE=24` - Batch size
- `USE_VGG=true` - 是否使用 VGG
- `LEARNING_RATE=1e-4` - 學習率

---

## 🔍 檢查點檢測邏輯

腳本會按以下順序查找檢查點：

1. **優先**: `checkpoints_improved/checkpoint_epoch_*.pth`（按時間排序，取最新的）
2. **備選**: `checkpoints_improved/best_model.pth`
3. **最後**: 如果都沒有，從頭開始訓練

---

## 💡 使用示例

### 示例 1: 快速繼續訓練

```bash
./延續訓練_自動檢查點.sh
```

### 示例 2: 在 tmux 中運行

```bash
tmux new -s arwgan_training
cd /mnt/nvme/p3/Project/arwgan/ARWGAN-Project/ARWGAN-main
./延續訓練_自動檢查點.sh
# Ctrl+b, d 分離會話
```

### 示例 3: 查看會使用哪個檢查點

```bash
cd /mnt/nvme/p3/Project/arwgan/ARWGAN-Project/ARWGAN-main
LATEST=$(ls -t checkpoints_improved/checkpoint_epoch_*.pth 2>/dev/null | head -1)
echo "將使用檢查點: $LATEST"
```

---

**最後更新**: 2026-01-29
