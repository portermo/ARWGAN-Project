# ARWGAN 虛擬環境安裝完成總結

## ✅ 已成功建立的虛擬環境

虛擬環境位置: `/mnt/nvme/Project/arwgan/ARWGAN-Project/ARWGAN-main/venv`
Python 版本: Python 3.10.19

## 📦 已安裝的套件

### 核心套件
- **torch**: 1.12.1+cu102 (CUDA 10.2 版本)
- **torchvision**: 0.13.1+cu102
- **numpy**: 2.2.6 (由 torchvision 自動安裝)
- **Pillow**: 12.1.0 (由 torchvision 自動安裝)
  - ⚠️ 注意: README 要求 Pillow 7.2.0，但已安裝 12.1.0
  - 通常較新版本的 Pillow 向後相容，應該可以正常運作
  - 如果遇到問題，可能需要降級到 7.2.0（需要編譯依賴）

### 其他依賴套件
- **kornia**: 0.6.12 (與 PyTorch 1.12.1 相容的版本)
- **tensorboard**: 2.20.0

## 📝 注意事項

### Pillow 版本差異
- README 要求: Pillow 7.2.0
- 實際安裝: Pillow 12.1.0 (由 torchvision 自動安裝)
- 建議: 先測試程式是否能正常運作。如果遇到相容性問題，可以嘗試：
  ```bash
  source venv/bin/activate
  pip install Pillow==7.2.0
  ```
  注意：安裝 Pillow 7.2.0 可能需要編譯依賴，如：
  ```bash
  sudo apt install libjpeg-dev zlib1g-dev
  ```

### kornia 版本
- 已安裝 kornia 0.6.12（與 PyTorch 1.12.1 相容）
- 較新版本的 kornia 需要 PyTorch >= 2.0.0，因此使用舊版本

### tensorboard_logger
- 程式碼中使用了 `tensorboard_logger` 模組
- 如果執行時出現 import 錯誤，可能需要安裝：
  ```bash
  source venv/bin/activate
  pip install tensorboardX
  ```

## 🚀 使用虛擬環境

### 啟動虛擬環境
```bash
cd /mnt/nvme/Project/arwgan/ARWGAN-Project/ARWGAN-main
source venv/bin/activate
```

### 退出虛擬環境
```bash
deactivate
```

### 驗證安裝
```bash
source venv/bin/activate
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "import torchvision; print(f'Torchvision: {torchvision.__version__}')"
python -c "import kornia; print(f'Kornia: {kornia.__version__}')"
python -c "import PIL; print(f'Pillow: {PIL.__version__}')"
```

## 🔧 如果遇到問題

### 問題: 找不到 tensorboard_logger
```bash
source venv/bin/activate
pip install tensorboardX
```

### 問題: Pillow 版本不相容
```bash
source venv/bin/activate
# 安裝編譯依賴
sudo apt install libjpeg-dev zlib1g-dev
# 降級 Pillow
pip install Pillow==7.2.0
```

### 問題: CUDA 相關錯誤
- 確保系統已安裝 CUDA 10.2 或相容版本
- 檢查 CUDA 是否可用：
  ```bash
  source venv/bin/activate
  python -c "import torch; print(torch.cuda.is_available())"
  ```

## 📚 相關文件

- `requirements.txt`: 套件清單
- `環境設定說明.md`: 詳細的安裝說明
- `setup_venv.sh`: 自動安裝腳本（需要調整以使用 Python 3.10）