# 根據 README 和程式碼分析所需的套件
# 
# ⚠️ 重要: Python 版本相容性問題
# README 要求 Python 3.7.4，但 PyTorch 1.12.1 只支援到 Python 3.10
# 如果使用 Python 3.11+，需要安裝較新版本的 PyTorch (可能會有相容性問題)
#
# 建議: 使用 Python 3.7-3.10 建立虛擬環境以符合原始要求
# 如果必須使用 Python 3.11+，請安裝較新版本的 PyTorch

# 核心套件
Pillow>=9.0.0,<11.0.0  # Pillow 12.x 與 Python 3.10 enum 不兼容，使用 10.x 版本
numpy<2.0  # 重要: 必須使用 1.x 版本以相容 PyTorch 1.12.1
kornia<0.7  # 使用與 PyTorch 1.12.1 相容的版本
tensorboard
kagglehub

# PyTorch 安裝說明:
# - 推薦使用 PyTorch 2.x 以支援新款 GPU (如 RTX 4090):
#   pip install torch==2.0.1 torchvision==0.15.2 --index-url https://download.pytorch.org/whl/cu118
# - 或使用升級腳本:
#   ./upgrade_pytorch.sh
# 
# - 舊版本 PyTorch 1.12.1 (不支援 RTX 4090):
#   pip install torch==1.12.1+cu102 torchvision==0.13.1+cu102 --extra-index-url https://download.pytorch.org/whl/cu102

# 注意: 程式碼中使用了 tensorboard_logger，這可能需要額外的套件
# 如果遇到 import 錯誤，可能需要安裝: pip install tensorboardX